# Showdown frontend

Flutter web client. See the repository root for the full project brief and
`../docs/STATUS.md` for the current state.

Development: `make dev-web` from the repository root (expects `make dev-api`
on port 8080).
